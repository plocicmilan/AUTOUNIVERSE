{
  "name": "autouniverse-garage-mobile",
  "version": "1.42.0",
  "private": true,
  "description": "Capacitor wrapper za Garage Toolbox PWA (Google Play distribucija)",
  "scripts": {
    "build:www": "node ../build_www.js garage",
    "sync": "npm run build:www && cap sync android",
    "open:android": "cap open android",
    "assemble:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
